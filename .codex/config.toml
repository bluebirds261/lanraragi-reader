[agents]
max_threads = 4
max_depth = 1
job_max_runtime_seconds = 1800

[agents.android-ui]
description = "Implements bounded Jetpack Compose UI and interaction changes in LANraragi Reader."
config_file = "agents/android-ui.toml"

[agents.data-api]
description = "Implements LANraragi Reader repositories, local archive I/O, Coil loading, and server API integration after checking the local 0.9.81 server source."
config_file = "agents/data-api.toml"

[agents.reviewer]
description = "Reviews integrated LANraragi Reader changes for regressions, state ownership, lifecycle safety, API correctness, and missing verification."
config_file = "agents/reviewer.toml"
